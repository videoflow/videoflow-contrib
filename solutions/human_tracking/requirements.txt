# torch/torchvision are installed first (see Dockerfile) because detectron2 needs them
# at build time. tensorflow (Keras 2) backs the humanencoder node; scipy backs deepsort.
torch>=2.1
torchvision>=0.16
tensorflow>=2.13,<2.16
scipy>=1.10

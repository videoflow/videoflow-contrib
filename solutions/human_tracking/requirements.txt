# torch is installed via the PyTorch index in the Dockerfile (CPU wheels); detectron2
# is built from source there. tensorflow (frozen-graph humanencoder) + scipy (deepsort)
# come from here.
tensorflow>=2.16,<2.18
scipy>=1.11
